# Hoist nothing by default; workspace packages are linked from source.
link-workspace-packages=true
prefer-workspace-packages=true
# Keep lockfile deterministic for judges cloning the repo.
resolution-mode=highest
# Type packages must resolve from every workspace package, not just the root.
public-hoist-pattern[]=@types/*
